# Project 2: CSV Data Cleaner + Report Generator

    Build a script that ingests a messy CSV (missing rows, wrong types), cleans it with pandas, and outputs a structured summary report. Format: CLI + output file.

    Covers: Data reshaping (dropna, filtering, dtype correction), pathlib for file I/O, structured logging instead of print statements.

    Swiss value: Data cleaning is 70% of real ML work. Recruiters at Swiss banks and pharma firms (Roche, Novartis) test this implicitly.
